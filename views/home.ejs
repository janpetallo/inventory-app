<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (typeof vinyls !== 'undefined' && vinyls.length > 0) { %>
    <ul>
      <% vinyls.forEach(vinyl => { %>
      <li>
        <h2><%= vinyl.title %></h2>
        <p>Artist: <%= vinyl.artist_name %></p>
        <p>Genre: <%= vinyl.genre_name %></p>
        <p>Release Year: <%= vinyl.release_year %></p>
        <p>Stock Quantity: <%= vinyl.stock_quantity %></p>
        <p>Price: $<%= vinyl.price %></p>
        <button>
          <a href="/vinyls/<%= vinyl.title %>/<%= vinyl.id %>/edit">Edit</a>
        </button>
        <form
          action="/vinyls/<%= vinyl.title %>/<%= vinyl.id %>/delete"
          method="POST"
          style="display: inline"
        >
          <button
            type="submit"
            onclick="return confirm('Are you sure you want to delete this vinyl?');"
          >
            Delete
          </button>
        </form>
        <br />
        <img src="<%= vinyl.cover_image %>" alt="<%= vinyl.title %> cover" />
      </li>
      <% }); %>
    </ul>
    <% } else { %>
    <p>No vinyls found.</p>
    <% } %>
    <p><a href="/vinyls/new">Add New Vinyl</a></p>
  </body>
</html>
